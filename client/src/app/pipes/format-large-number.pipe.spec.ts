import { FormatLargeNumberPipe } from './format-large-number.pipe';

describe('FormatLargeNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLargeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
